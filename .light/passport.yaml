project: SiftSC
version: 0.1.0
source_project: ICONIP paper workspace (local, not vendored)
source_mutated: false
current_stage: complete
stages:
  - name: intake
    status: PASS
    artifacts:
      - docs/TERMINOLOGY.md
    evidence: paper method, source modules, cached records, local models, and GitHub account audited
  - name: implementation
    status: PASS
    artifacts:
      - src/siftsc
      - tests
      - examples
      - pyproject.toml
    evidence: unit tests, lint, strict type check, and package build
  - name: benchmark_packaging
    status: PASS
    artifacts:
      - src/siftsc/profiles
      - docs/benchmarks
      - docs/assets/help-harm.png
      - docs/assets/pooled-operating-points.png
    evidence: rebuilt from original cached records with SHA-256 provenance
  - name: local_model_validation
    status: PASS
    artifacts:
      - docs/benchmarks/local_mlx_smoke.json
    evidence: Qwen-0.5B MLX-Q4 one-pass and forced SC@5 paths both executed
  - name: ethics
    status: PASS_WITH_NOTES
    artifacts:
      - docs/ETHICS_REVIEW.md
    evidence: no raw prompts, completions, weights, secrets, or paper files included
  - name: private_release
    status: PASS
    artifacts:
      - https://github.com/heywanrong/SiftSC
    evidence: private repository created; Python 3.11, 3.12, and 3.13 CI matrix passed on GitHub Actions run 34588635034
  - name: readme_experience
    status: PASS
    artifacts:
      - README.md
      - docs/assets/hero.svg
      - src/siftsc/cli.py
    evidence: public 290 MB Hugging Face model auto-download, interactive mode switching, and a plain-wrong/SiftSC-right demo all reproduced locally
  - name: compute_visibility
    status: PASS
    artifacts:
      - README.md
      - src/siftsc/cli.py
      - docs/benchmarks/RESULTS.md
    evidence: CLI reports per-request and cumulative generation-pass savings; public-model demo shows both beneficial and harmful voting cases plus 79.2% workload compute reduction
  - name: brand_and_readme_polish
    status: PASS
    artifacts:
      - README.md
      - docs/assets/logo.png
    evidence: compact transparent logo added; README hierarchy polished around one command, two real behaviors, and two headline numbers
  - name: onboarding_clarity
    status: PASS
    artifacts:
      - README.md
      - docs/assets/logo.png
    evidence: cartoon mascot moved beside the wordmark; Apple-Silicon limitation is disclosed above the fold; badges and cards clarify install, modes, benefits, and method
  - name: private_install_resilience
    status: PASS
    artifacts:
      - README.md
      - tests/test_release_assets.py
    evidence: exact GitHub install succeeded in a clean Python 3.13 environment; demo reproduced both reference cases; README distinguishes port-443 connectivity, private-repo authentication, and local-clone fallback
  - name: interactive_terminal_experience
    status: PASS
    artifacts:
      - src/siftsc/cli.py
      - src/siftsc/terminal.py
      - README.md
    evidence: demo continues into a reusable question loop on TTYs; local Qwen test voted on the Henry prompt, returned 25, reported six passes, and removed repeated model-turn text
  - name: cli_chat_experience
    status: PASS
    artifacts:
      - src/siftsc/cli.py
      - src/siftsc/display.py
      - src/siftsc/terminal.py
      - src/siftsc/backends.py
      - README.md
    evidence: pseudo-terminal runs at 80 columns with a fresh Hugging Face cache showed download progress, no leaked library output, the demo answering typed questions, compare mode, /stats charts, and Ctrl-C recovery; unit, lint, format, and strict type gates passed
known_limitations:
  - one generation seed per experiment cell
  - thresholds selected and evaluated on the same out-of-fold score pool
  - bundled profiles require matching model, precision, prompt, and decoding distribution
next_action: maintainer review of the charted terminal experience
